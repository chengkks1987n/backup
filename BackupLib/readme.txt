1. description 
   this is the library needed by the BackupWeb, apply the function to operate 
the database, the interface to backup/restore the table records.

2. files structure
   this sub-project uses the maven build-tools, the files structure is 
according to maven files structure.	  	  
   makefile : show how to build the project use command line, let me know the
detail of the build processes

